Additional checkpoint validation for PR40384, using the same committed kernel. No extra PR is counted. All raw timed rows are retained, including client outliers. Profiles are diagnostic, excluded from performance means. Each step slice covers a complete positive/negative CFG iteration with zero GPU boundary crossings. The T2I BCG trace records four CFG iterations (eight model calls, 248 graph launches). An initial cleanup audit incorrectly expected all 50 iterations; retry1 corrects only that offline audit assertion and performs the verified weight deletion. Automated nvjet-to-FP8 triage advice is rejected because these GEMMs are BF16. Both candidate images were visually inspected and match the intended prompt.
