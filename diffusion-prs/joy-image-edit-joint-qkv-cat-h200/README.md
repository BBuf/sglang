Native Joy Image Edit on two H200 GPUs, CFG parallel 2, TP1/SP1: lossless eager strided image QK/RoPE plus image/text QKV concatenation in one pure-copy kernel.

The final-evidence.json audit retains copy-only v2/final and combined outplace-v1 full-40-step-warmup ABBA groups. The copy-only final round 1 missed the 1.5% worker/client threshold; the combined path removes two image Q/K input copies per block. Combined oneshot round 2 also missed the client threshold because of a retained 16.08 s saved-client outlier (13.824 s worker). The persistent-v1 protocol was declared before running: two fixed ABBA groups, each fresh native generator process has a native 40-step request warmup, then one full saved-output warmup and five measured saved requests. All 40 measured observations and 8 saved warmups are preserved. Those worker-save persistent groups also failed saved-client qualification, retaining a16.99s candidate outlier. Separate diagnostic worktrees identified a1.76s rank0 empty_cache delay in baseline; candidate did not reproduce its prior tail in twelve diagnostic requests. Only the later client-save-v1 fixed groups supply the headline, with native return_file_paths_only=False and save_output=True in both arms. Generation, returned pixel transport, PNG saving and reporting remain timed; no allocator or GC function is patched. No default-file-path client improvement is claimed and the cause of every earlier individual outlier is not proven. The earlier single-request screen was below the end-to-end threshold. Small eager microbenchmarks regressed before the 32 MiB model guard; their original measurements remain.

Native BCG reports disabled; its saved eager fallback images are independently hashed and excluded from timing claims. Standalone CUDA Graph is a kernel test/diagnostic. The high mode is also an output-correctness check. Original baseline/candidate images are native saved PNGs; the comparison grid only resizes them for display.

The initial native command failed because --cfg-parallel-degree was not a supported CLI flag; retry1 uses --cfg-parallel-size. A stale campaign-owned inspector waiter was terminated before the corrected inspector ran. The first persistent harness did not supply sys.argv to native from_cli_args and failed before model initialization; retry1 preserves native explicit-argument parsing. Failed logs and markers remain. All model weights were removed only after final evidence and visual review, with zero remaining owned cache files/weights/bytes.

See ncu/REPORT.md for hardware counters and their limits; do not infer reduced DRAM bytes from the speedup. Profiles contain full and CUDA-launch-correlated complete-forward slices. The generic analyzer's GEMM/MoE classifications require the corrections recorded in final-evidence.json.
