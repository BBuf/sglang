# Joy saved-request tail diagnosis

The fixed combined oneshot and persistent measurements remain unchanged. The persistent protocol completed 48 saved requests: eight predeclared full saved warmups and forty measured requests. Both saved-client groups fail the fixed qualification threshold; all source/worker/image evidence is retained. Model weights remain present for diagnosis.

Diagnostics use separate detached worktrees with a tiny timing-only patch around materialization, peak-memory recording/collective, empty_cache, performance summary/report writes, plus GC callbacks. They are not submitted code and never supply performance qualification. Each arm runs twelve saved requests with the same native model parameters and output-hash assertion.

Preparation history: the first waiting diagnostic driver was stopped before any GPU request to add GC callbacks. Retry1 created the baseline worktree but could not commit because the remote repository lacked author identity. Retry2 resumes that exact staged patch with per-command BBuf identity copied from the local repository, leaving global Git configuration unchanged. Baseline diagnostic commit is833c404c6ee53f8da907d577c58f3f609e51eae8 (parent80da4432). The runner's directory-replacement expression did not match the original joined path literal: diagnostic results reside under `persistent/baseline` and `persistent/candidate`. Their distinct source commits/labels and logs identify them; the final formal auditor uses only the predeclared label list and excludes them. Media packaging places them under `raw/tail-diagnostic` without modifying original records.

Completed diagnosis: baseline request index 6 takes 14.238 s in the worker and 16.160 s at the client. Rank 0 spends 1.760153 s in empty_cache; rank 1 spends 1.920772 s there. Baseline PNG saving has a median of 0.155220 s and a maximum of 0.160189 s; output-rank empty_cache has a median of 0.005360 s. Peak-memory collection and report writes remain below one millisecond. This directly accounts for the observed baseline diagnostic tail.

The twelve candidate diagnostic requests have worker times of 13.743–13.833 s and client times of 13.909–14.014 s. No comparable tail reproduced: output-rank empty_cache peaks at 0.021488 s, and PNG saving peaks at 0.159662 s. Startup/shutdown generation-2 GC pauses are separated from request attribution in request-tail-diagnostic-final.json; the original analyzer report is retained. The allocator explanation for every earlier candidate outlier remains an inference, not a demonstrated attribution. All diagnostic PNG hashes agree with the original request.

The subsequent fixed client-save protocol selects the existing native return_file_paths_only=False and save_output=True in both revisions. Retaining pixel data through the worker response avoids its existing output-is-None empty_cache branch; native transport, client PNG saving and reporting remain inside the measured request. No production allocator or GC function is changed. The output configuration is part of the benchmark scope, and the default worker-save path's failed results remain published. Final qualification is determined only by the uninstrumented, predeclared client-save groups, never by these diagnostic requests.
